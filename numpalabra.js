const numPalabra = function (pal, num) {
  for (let i = 1; i <= num; i++) {
    console.log(i + ": " + pal);
  }
};

console.log(
  'Escribe numPalabra("a",b) , cambiando a por una palabra y b por la cantidad de veces que quieres que se repita la palabra'
);
