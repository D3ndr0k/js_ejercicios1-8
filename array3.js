const tabla = function (array = [], x) {
  for (let i = 0; i < array.length; i++) {
    console.log(array[i] * x);
  }
};

console.log(
  "Escribe: tabla(array= [a,b,c...], x), cambiando a, b, c...  por todos los números que quieras multiplicar y x por el multiplicador"
);
